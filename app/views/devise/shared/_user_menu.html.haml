- if user_signed_in?
  #{t('user_menu.welcome')} #{current_user.first_name}, #{link_to t('auth.log_out'), destroy_user_session_path, method: :delete}

- else
  =link_to t('auth.log_in'), user_session_path
